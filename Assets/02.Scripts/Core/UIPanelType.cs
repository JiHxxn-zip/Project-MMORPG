/// <summary>
/// 프로젝트에서 관리하는 모든 UI 패널 종류를 정의하는 열거형.
/// </summary>
namespace MMORPG.Core
{
    public enum UIPanelType
    {
        None = 0,
        Logo,
        Main,
        Dialogue,
        QuestTracker,
        Loading,
    }
}
